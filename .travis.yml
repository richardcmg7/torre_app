language: node_js
node_js:
  - 'node'

branches:
  only:
  - master

addons:
  chrome: stable

before_script:
  - yarn global add @angular/cli

cache:
  yarn: true
  directories:
    - node_modules

script:
  - ng build --prod

deploy:
  provider: pages
  skip-cleanup: true
  github-token: 74063a3e99f32f6e0b071b0d1b6fc2465bb6de13
  keep-history: true
  local-dir: ./dist/
  on:


